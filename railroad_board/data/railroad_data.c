#include "expansion_data.c"

const GameData_t load_expansions(size_t expansion_amount, ...) {
    /* TODO */
    return NULL;
}

const Tile_t* load_tile(const GameData_t game_data, size_t expansion_index, size_t local_index) {
    return NULL;
}
